# Generated by Okty.io
version: '3.6'
services:
    postgresql:
        image: 'postgres:10.4'
        ports:
            - '3306:5432'
        environment:
            - PGDATA=/var/lib/postgresql/data/pgdata
            - POSTGRES_DB=docker
            - POSTGRES_USER=user
            - POSTGRES_PASSWORD=password
